name: Press Notification

on:
  push:
    paths:
      - "**/press.html"
      - "**/press.xml"

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord notification
        run: |
          curl -X POST -H "Content-Type: application/json" \
          -d '{"content":"📢 New Moeezland press release published"}' \
          ${{ secrets.DISCORD_WEBHOOK }}
