name: Press Notification

on:
  push:
    paths:
      - "**/press.html"

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord press notification
        run: |
          curl -X POST "${{ secrets.DISCORD_WEBHOOK }}" \
          -H "Content-Type: application/json" \
          -d '{"content":"📢 New official press release published by the Government of Moeezland\nhttps://moeezland.com/press.html"}'
