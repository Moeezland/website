name: Webhook Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -X POST -H "Content-Type: application/json" \
          -d '{"content":"🚨 Webhook test — if you see this, it WORKS"}' \
          ${{ secrets.DISCORD_WEBHOOK }}
